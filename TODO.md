1. Support more than one Release Pipeline
2. Monitor k8s for pods that do not come up after deployment (e.g. wrong image, or other reason for termination)
3. Support Proxmox (both images and workloads on VMs).
4. Export release history
5. Support relationship based access control to restrict users to see only part of the data.
6. Implement cdrm-manager